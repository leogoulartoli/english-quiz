import styled from "styled-components";

const IntroText = styled.h4`
  color: ${({textIntroColor})=>textIntroColor};
  font-weight: 500;
  font-size: 1.2rem;
  margin: 1.5rem 0;
`;

export default IntroText;